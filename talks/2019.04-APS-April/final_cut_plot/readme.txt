You will need ROOT 6

In the presence of the Makefile, final_cut_plot.cc and the *.root files, first make the executable:

`make final_cut_plot`

The run `./final_cut_plot -14 10.5`. -14 is the slope of the line, 10.5 is the y-intercept.